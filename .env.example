# 哨兵 AI 助手 — 环境变量样例
# 复制本文件为 .env.local (本地开发) 或在 Vercel 控制台 Settings → Environment Variables 配置
# .env.local 不会被 git 提交 (已在 .gitignore)

# ── DeepSeek API (Agent 2 LUI 反问的语义相似度判断) ────────────
# 申请: https://platform.deepseek.com/api_keys
# 不填: /api/similarity 自动 fallback 到 mock 分数, demo 仍可正常演示
DEEPSEEK_API_KEY=
